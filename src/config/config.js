// src/config/config.js
module.exports = {
  distubeOptions: {
    // Estas são as opções que o seu PlayerManager irá usar.
    // Sinta-se à vontade para adicionar outras opções do DisTube aqui.
    emitNewSongOnly: true,
    emitAddSongWhenCreatingQueue: false,
    emitAddListWhenCreatingQueue: false,
  }
};